# vectorbt/indicators/factory.py 详细分析

## 文件设计逻辑与作用总结

本文件是vectorbt库中指标系统的核心组件，实现了一个强大的指标工厂模式。

### 主要功能模块：

1. **参数处理系统**
   - params_to_list(): 将参数转换为列表格式
   - prepare_params(): 准备和预处理参数列表
   - build_columns(): 构建多级列索引

2. **管道执行引擎**  
   - run_pipeline(): 运行指标管道的核心函数
   - combine_objs(): 组合多个对象的工具函数

3. **指标基类系统**
   - MetaIndicatorBase: 指标基类的元类
   - IndicatorBase: 所有指标的基类

4. **指标工厂类**
   - IndicatorFactory: 核心工厂类，动态创建指标类

### 核心设计特性：

1. **工厂模式**: 动态创建指标类
2. **管道模式**: 标准化的计算流程  
3. **广播机制**: 智能的数组形状处理
4. **性能优化**: 缓存、唯一运行、原始输出重用
5. **扩展性**: 支持自定义属性、第三方库集成

### 使用示例：



该文件通过精心设计的架构，为用户提供了一个强大、灵活且高性能的技术指标创建和使用系统。
